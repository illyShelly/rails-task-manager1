<%= form_for(@task) do |f| %>
<div class="form-input">
  <%= f.label :title %>
  <%= f.text_field :title, class: 'form-control' %>
</div>
<div class="form-input">
  <%= f.label :details %>
  <%= f.text_area :details, class: 'form-control' %>
</div>

<!-- part for edit form, bootsrap class checkbox -->
<% unless f.object.new_record? %>
    <div class="checkbox">
      <label>
        <%= f.check_box :completed %> Completed
      </label>
    </div>
  <% end %>

  <%= f.submit class: 'btn btn-primary' %>
<% end %>

<!-- text_area has 'detail' and 'class' - needs comma -->
<!-- class has different structure -->
<!-- put div to make some space -->
<!-- new_record?()  -->
<!-- Returns true if this object hasn't been saved yet – that is, a record for the object doesn't exist in the database yet; otherwise, returns false. -->
